import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    brand: {
      500: '#4A25E1', // Map your hex colors
      600: '#7B5AFF',
    },
  },
});

export default theme;
